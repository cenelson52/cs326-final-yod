/*

'use strict'

const tournament = *thing used to get tournament*


//TODO: Change tourney page to reflect this or change this to reflect tourney page. Depends on how Edit Tourney Button is ultimately set up.
function renderTournament(element){
    for(const x in tournaments){
        const newDiv = document.createElement("div");
        newDiv.addClass(game-list-item);
        newDiv.innerHTML = x.name;
        element.appendItem(newDiv);
        //use document.addEventListener for each tournament page
    }
}

document.getElementById('back-to-home').addEventListener('click', () => {
  <a href="*****Whatever the homepage url is****"></a>   //TODO: Look for a working example
});

//TODO: Edit Tourney Button


window.onload(()=>
    renderGames(document.getElementById("fulltournamentlist"));
);







*/
